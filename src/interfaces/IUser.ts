export interface IUserInfo {
    first_name: string
    last_name: string
    createdAt: string
    email: string 
    id: number
    name: string
    password: string
    role: string
    updatedAt: string
}
